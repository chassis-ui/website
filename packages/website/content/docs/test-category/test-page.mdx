---
title: Docs Layout Test Page
description: A comprehensive test page covering all docs layout features, shortcodes, and markdown elements.
toc: true
added:
  show_badge: true
  version: 0.1.0
aliases:
  - '/test-category'
  - '[[docsref:/test-page]]'
---

## Overview

This page exists to test the docs layout and all available content features. It covers frontmatter options, markdown elements, all shortcode components, code blocks, tables, and callouts.

---

## Badges

<AddedIn version="1.2.0" /> <DeprecatedIn version="2.0.0" /> <CSSOnly /> <InFigma />

## Blockquote

<Blockquote>Good design is as little design as possible.</Blockquote>

<Blockquote attr="Charles Eames">
  The details are not the details. They make the design.
</Blockquote>

## Callouts

<Callout type="info">
  This is an **info** callout with `inline code` and [link to docs]([[docsref:/test-category/test-page]]). Use it for general notes and tips.
</Callout>

<Callout type="warning">
  This is a **warning** callout with `inline code` and [link to docs]([[docsref:/test-category/test-page]]). Use it to highlight something important.
</Callout>

<Callout type="danger">
  This is a **danger** callout with `inline code` and [link to docs]([[docsref:/test-category/test-page]]). Use it for breaking changes or destructive
  actions.
</Callout>

## Code Blocks

Syntax-highlighted code blocks can be added using standard markdown fenced code blocks with language identifiers, or the `<Code>` shortcode for explicit control over formatting and display options.

### Markdown code

```html
<!-- A code block with syntax highlighting for HTML -->
<div class="container">
  <h1 class="font-5xlarge">Hello, Chassis</h1>
  <p class="fg-subtle">Token-powered UI system.</p>
</div>
```

```scss
// A code block with syntax highlighting for SCSS
.my-component {
  color: var(--cx-fg-main);
  background-color: var(--cx-bg-even);
  border: 1px solid var(--cx-border-subtle);
}
```

```ts
// A code block with syntax highlighting for TypeScript
import { getConfig } from '@libs/config'

const config = getConfig()
console.log(config.title)
```

```sh
# A code block with syntax highlighting for shell commands
pnpm install && pnpm dev
```

### Code shortcode

<Code lang="sh" code="pnpm install && pnpm dev" />

<Code
  lang="json"
  code={`{
  "chassis": {
    "build": {
      "brands": ["chassis", "example"],
      "apps": { "docs": ["web"] }
    }
  }
}`}
/>

## Examples

Live preview with markup using the `<Example>` shortcode.

### With markup

<Example
  code={`<button type="button" class="button primary">Primary</button>
<button type="button" class="button default">Default</button>
<button type="button" class="button danger">Danger</button>`}
/>

### Preview only

<Example
  showMarkup={false}
  code={`<button type="button" class="button primary">Primary</button>
<button type="button" class="button default">Default</button>
<button type="button" class="button danger">Danger</button>`}
/>

### With external files

<ScssDocs name="example-scss" file="content/docs/test-category/example.scss" />
<JsDocs name="example-js" file="content/docs/test-category/example.js" />

## Placeholders

<Placeholder class="image fluid mb-medium" text="SVG Element" />  

<Placeholder markup="img" width="100%" height="180" class="mb-medium object-fit-cover" text="IMG Element" />

## Tables

<CxTable class="table table-striped">
| Variable             | Default          | Description               |
| -------------------- | ---------------- | ------------------------- |
| `--cx-fg-main`       | `#1a1a1a`        | Primary foreground colour |
| `--cx-bg-main`       | `#ffffff`        | Primary background colour |
| `--cx-border-subtle` | `rgba(0,0,0,.1)` | Subtle border colour      |
| `--cx-primary`       | `#0066ff`        | Brand primary colour      |
</CxTable>

