---
/**
 * Feature Slider
 *
 * Swiper-based carousel wrapper with navigation and pagination controls.
 * Displays feature cards in a responsive slider layout.
 *
 * @slot default - Feature cards to display in the slider
 */
interface Props {
  /** Accessible label for the slider region */
  ariaLabel?: string
}

const { ariaLabel = 'Feature carousel' } = Astro.props
---

<div class="feature-slider">
  <div class="swiper-navigation d-none md:d-block" role="group" aria-label="Slider navigation">
    <button type="button" class="swiper-button-prev button" aria-label="Previous slide"></button>
    <button type="button" class="swiper-button-next button" aria-label="Next slide"></button>
  </div>
  <div class="slider-swiper overflow-visible swiper" role="region" aria-label={ariaLabel}>
    <div class="swiper-wrapper flex-column md:flex-row gap-md md:gap-0">
      <slot />
    </div>
  </div>
  <div class="swiper-pagination" role="group" aria-label="Slide pagination"></div>
</div>
