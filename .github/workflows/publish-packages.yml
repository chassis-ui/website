name: Publish Packages

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      docs-changed: ${{ steps.changes.outputs.docs-changed }}
      docs-version: ${{ steps.version.outputs.docs-version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 2 # Need at least 2 commits to compare

      - name: Check for package changes
        id: changes
        run: |
          # Check if docs package.json version changed
          if git diff HEAD~1 HEAD --name-only | grep -q "packages/docs/package.json"; then
            CURRENT_VERSION=$(node -p "require('./packages/docs/package.json').version")
            PREVIOUS_VERSION=$(git show HEAD~1:packages/docs/package.json | node -p "JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')).version")

            echo "Current version: $CURRENT_VERSION"
            echo "Previous version: $PREVIOUS_VERSION"

            if [ "$CURRENT_VERSION" != "$PREVIOUS_VERSION" ]; then
              echo "docs-changed=true" >> $GITHUB_OUTPUT
            else
              echo "docs-changed=false" >> $GITHUB_OUTPUT
            fi
          else
            echo "docs-changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Get current version
        id: version
        run: |
          VERSION=$(node -p "require('./packages/docs/package.json').version")
          echo "docs-version=$VERSION" >> $GITHUB_OUTPUT

  publish-docs:
    needs: detect-changes
    if: needs.detect-changes.outputs.docs-changed == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: pnpm install

      - name: Check if version already published on npm
        id: npm-check
        run: |
          VERSION="${{ needs.detect-changes.outputs.docs-version }}"
          if npm view "@chassis-ui/docs@$VERSION" version 2>/dev/null | grep -q "$VERSION"; then
            echo "already-published=true" >> $GITHUB_OUTPUT
            echo "Version $VERSION already on npm, skipping publish"
          else
            echo "already-published=false" >> $GITHUB_OUTPUT
            echo "Version $VERSION not on npm, will publish"
          fi

      - name: Publish @chassis-ui/docs
        if: steps.npm-check.outputs.already-published == 'false'
        working-directory: packages/docs
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_CHASSIS_DOCS }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "@chassis-ui/docs@${{ needs.detect-changes.outputs.docs-version }}"
          name: "@chassis-ui/docs v${{ needs.detect-changes.outputs.docs-version }}"
          body: |
            Published @chassis-ui/docs v${{ needs.detect-changes.outputs.docs-version }} to npm.

            Install with:
            ```bash
            npm install @chassis-ui/docs@${{ needs.detect-changes.outputs.docs-version }}
            ```
          draft: false
          prerelease: false
